{
  "name": "fe",
  "version": "1.0.0",
  "description": "We connect students with nonprofit causes worth working for.",
  "author": "ForEach",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.0",
    "node-sass": "^4.7.2"
  },
  "scripts": {
    "start": "live-server static",
    "compile:sass": "node-sass static/sass/main.scss static/css/main.css -w --source-map true"
  }
}
